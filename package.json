{
  "name": "doctor-finder-back-end",
  "engines": {
    "node": "10.4.0"
  },
  "version": "1.0.0",
  "description": "Provides API for doctor-finder",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node --experimental-modules app.mjs",
    "dev": "nodemon --experimental-modules app.mjs",
    "heroku-postbuild": "cd front-end && npm install && npm run build"
  },
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@koa/cors": "^3.1.0",
    "koa": "^2.12.0",
    "koa-body": "^4.1.3",
    "koa-router": "^9.0.1",
    "koa-static": "^5.0.0",
    "lodash.chunk": "^4.2.0",
    "node-fetch": "^2.6.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.4"
  }
}
